#include "main.h"
/**
 * _islower - checks if c is lowercase letter
 * @c: character to be checked
 * Return: 1 if c is lowercase,
 *return: 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
