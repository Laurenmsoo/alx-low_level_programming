#include "main.h"

/**
 * _islower - checks if character is lowercase
 * @c: character to check
 * Return: 1 if c is lowercase
 * return 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

