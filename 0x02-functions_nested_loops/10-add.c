#include "main.h"
/**
*add - adds two integers, a and b, and returns the sum
*@a: int to be added to b
*@b: int to be added to a
*Return: sum of a and b
*/
int add(int a, int b)
{
int sum;

sum = a + b;

return (sum);
}
